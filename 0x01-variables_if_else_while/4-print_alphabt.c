#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int lowercase = 'a';

	while (lowercas <= 'z')
	{
		if lowercase == 'e' || lowercase == 'q'
			lowercase += 1;
	}
	else
	{
		putchar(lowercase);
		lowercase += 1;
	}
}
putchar('\n');
return (0);
}

